$(document).ready(function(){

    //Pusher.logToConsole = true;

    /*var pusher = new Pusher('1153595b89e97452da79', {
      cluster: 'ap2'
    });

    var channel = pusher.subscribe('partnerzone');

    channel.bind('message_progress', function(data) {

      $("#"+data.not_id).find( ".notification-msg" ).html(data.progress['success'] + data.progress['fails'].length + ' sent of ' + data.progress['total']);

    });
    */

})